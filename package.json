{
  "name": "burn",
  "version": "0.1.0",
  "description": "Local-first AI usage and limits observability",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile dist/burn"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}
